#include <iostream>
#include <vector>

#include "NormalData.h"

using namespace std;

class Solution {
public:
    void nextPermutation(vector<int>& nums) {
    }
};

int main()
{
    Solution sln;
    int a[] = {1, 2, 3};
    vector<int> nums(a, a + SIZE(a));
    printVector<int>(nusm);
    sln.nextPermutation(nums);
    printVector<int>(nums);
    return 0;
}
