#include <isotream>
#include <string>

class Solution {
public:
    string fractionToDecimal(int numerator, int denominator) {
    }
};

int main()
{
    Solution sln;
    int numerator = 0, denominator = 0;
    cout << "Please enter numerator: ";
    cin >> numerator;
    cout << "Please enter denominator: ";
    cin >> denominator;
    cout << sln.fractionToDecimal(num, denominator);
    return 0;
}
