#include <iostream>
#include <vector>

#include "NormalData.h"

using namespace std;

class Solution {
public:
    vector<vector<int> > threeSum(vector<int> &num) {
    }
};

int main()
{
    Solution sln;
    vector<int> num;
    vector<vector<int> > v = sln.threeSum(num);
    print2DVector<int>(v);
    return 0;
}

