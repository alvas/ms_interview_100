#include <iostream>
#include <string>

using namespace std;

class Solution {
public:
    bool isMatch(string s, string p) {
    }
};

int main()
{
    Solution sln;
    string s, p;
    cout << sln.isMatch(s, 0) << endl;
    return 0;
}
