#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<pair<int, int>> getSkyline(vector<vector<int>>& buildings) {
                
    }
};

int main()
{
    Solution sln;
    vector<vector<int> > buildings;
    vector<pair<int, int> > v = sln.getSkyline(buildings);
    printVector<pair<int, int> >(v);
    return 0;
}
