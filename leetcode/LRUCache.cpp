#include <iostream>

using namespace std;

class LRUCache{
public:
    LRUCache(int capacity) {

    }

    int get(int key) {

    }

    void set(int key, int value) {

    }
};

int main()
{
    LRUCache cache(0);
    cache.set(0, 0);
    cout << cache.get(0) << endl;
    return 0;
}
