#include <iostream>
#include <vector>

#include "NormalData.h"

using namespace std;

class Solution {
public:
    int candy(vector<int>& ratings) {
        int minV = 0;
        return minV;
    }
};

int main()
{
    Solution sln;
    int a[] = {};
    vector<int> v(a, a + SIZE(a));
    cout << sln.candy(v) << endl;
    return 0;
}
