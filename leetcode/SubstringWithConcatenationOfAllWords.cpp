#include <iostream>
#include <string>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> findSubstring(string s, vector<string>& words) {
                
    }
};

int main()
{
    Solution sln;
    string s;
    vector<string> words;
    vector<int> v = sln.findSubstring(s, words);
    printVector<int>(v);
    return 0;
}
