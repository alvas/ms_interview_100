#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> findOrder(int numCourses, vector<pair<int, int>>& prerequisites) {
    }
};

int main()
{
    Solution sln;
    int num = 0;
    cout << "Please enter num of courses: "
    cin >> num;
    vector<pari<int, int> > pre;
    vector<int> v = sln.findOrder(num, pre);
    printVector<int>(v);
    return 0;
}
