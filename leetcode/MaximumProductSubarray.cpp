#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int maxProduct(vector<int>& nums) {
    }
};

int main()
{
    Solution sln;
    int a[] = {};
    vector<int> nums(a, a + SIZE(a));
    cout << sln.maxProduct(nums) << endl;
    return 0;
}
